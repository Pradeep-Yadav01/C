//Declaration type Instruction 

#include <stdio.h>
#include <math.h>
int main() {

    int age = 22; 
    int Oldage = age;
    int Newage = Oldage + 2;
    printf("New age is : %d", Newage);

    return 0;
}